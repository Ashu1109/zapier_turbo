import React from 'react'
import {Button} from "@repo/ui/button"
const page = ():JSX.Element => {
  return (
    <div>
      <Button>
        Done
      </Button>
    </div>
  )
}

export default page
